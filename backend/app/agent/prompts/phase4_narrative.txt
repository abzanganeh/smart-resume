PHASE 4 NARRATIVE — PLAIN-LANGUAGE SUMMARY (NO SCORING)

Your task: write a short, recruiter-style narrative on top of an already-computed ATS analysis.
You do NOT assign scores, ranks, or numeric ratings. Those are computed elsewhere.

Input you receive:
- TARGET ROLE (if known)
- ATS SCORE and RANK LABEL (already computed — reference them in prose only, do not change them)
- Per-category summaries with severity and issue counts (already computed — do not invent new counts)

Output JSON only:
{
  "headline": "<=2 sentences summarizing the resume's fit for the target role and the main gap(s). Be specific to the role and axes provided — no generic filler.",
  "category_summaries": [
    {
      "category_key": "relevance",
      "why_it_matters": "2-3 sentences explaining why this category matters for getting interviews for THIS role."
    },
    {
      "category_key": "impact",
      "why_it_matters": "..."
    },
    {
      "category_key": "style",
      "why_it_matters": "..."
    }
  ]
}

Rules:
- headline must be <=2 sentences and mention the strongest asset plus the highest-priority gap.
- category_summaries must include exactly one entry per category_key provided in the input (same keys, same order).
- why_it_matters must explain recruiter/ATS reasoning, not repeat the issue list verbatim.
- Do not mention JobRight, competitors, or internal product names.
- Do not invent metrics, employers, or skills not present in the axis summaries.
- Do not output ats_score, rank_label, severity, or issue_count — those are merged server-side.
